import { style } from '@vanilla-extract/css';

export const label = style({
  fontSize: 13,
  opacity: 0.5,
  fontWeight: 600,
  marginBottom: 4,
});

export const value = style({
  fontSize: 16,
  fontWeight: 700,
});
